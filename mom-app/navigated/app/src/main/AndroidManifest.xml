<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.srivikashini.navigated">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".login">

        </activity>
        <activity android:name=".signUp"></activity>

        <activity android:name=".changePassword"></activity>
        <activity android:name=".itemDecri"></activity>
        <activity android:name=".gridPreference"></activity>
        <activity android:name=".olivepreference"></activity>
        <activity android:name=".addKid"></activity>
        <activity android:name=".kidscladdr"></activity>
        <activity android:name=".signDecision">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>